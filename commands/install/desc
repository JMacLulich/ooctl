Install tmux if missing (Homebrew), then install the project in editable mode with
dev dependencies, set up shell autocomplete for bash, zsh, and fish, and install a
local launcher.

Usage:
  ./run install
